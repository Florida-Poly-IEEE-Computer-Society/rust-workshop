// main is the entry point for your code
fn main() {
    // This is a macro that would be used to print to console
    // If it were a function, you would not use a !
    // It would be println(input);
    println!("Hello, world!");
}
